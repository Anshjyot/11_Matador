package fields;

public class ParkingSquare extends Square { // Creates our VisitJailSquare field, and which functionality wise doesn't do anything
    public ParkingSquare(String fieldName) {
        super(fieldName);
    }
}