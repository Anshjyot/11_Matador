package fields;

public class FerrySquare extends Square { // Creates our VisitJailSquare field, and which functionality wise doesn't do anything
    public FerrySquare(String fieldName) {
        super(fieldName);
    }
}
