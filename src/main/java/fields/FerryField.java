package fields;

public class FerryField extends Field { // Creates our FerrySquare field, and which functionality wise doesn't do anything
    public FerryField(String fieldName) {
        super(fieldName);
    }
}
